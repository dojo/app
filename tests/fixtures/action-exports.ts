import { ActionLike } from 'src/createApp';

export const member1 = <ActionLike> {
	configure (configuration: Object) {}
};

export const member2 = <ActionLike> {
	configure (configuration: Object) {}
};
